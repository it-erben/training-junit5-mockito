# Einführung zu JUnit 5 – Ablauf

- `@Test`-Annotation
- `assertEquals`, auch mit Message und mit lazy Messages
- `@DisplayName`-Annotation
- `@Disabled`-Annotation
- `@Nested`-Tests
- `assertThrows` und `assertDoNotThrow`
