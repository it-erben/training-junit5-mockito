package org.example;

public class FibonacciCalculator {
    public long fibonacci(int index) {
        if (index < 0) {
            throw new IllegalArgumentException();
        }
        return -1;
    }
}
